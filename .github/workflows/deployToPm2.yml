
name: deployToPM2.js CI
on: 
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  
jobs: 
  build:
    runs-on: self-hosted
    
    steps:
    - uses:  actions/checkout@v2.5.0
    - run: |
        yarn
        npm run build
        npm run start
